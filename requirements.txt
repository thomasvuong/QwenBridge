# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
pydantic==2.10.3
httpx==0.28.1

# MCP Server (official Anthropic SDK)
mcp>=1.0.0

# Qwen / DashScope (OpenAI-compatible client)
openai>=1.58.1

# Alibaba Cloud (optional — falls back to local JSON in MOCK_MODE)
oss2>=2.19.0
tablestore>=5.4.0

# Observability
rich>=13.9.4

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# AgentWorld local inference (optional — only if running 35B model locally)
# transformers>=4.46.0
# accelerate>=1.2.0
